import {Plugin} from '../core/plugin';


/**
 * A Menu is an extendible plugin which allow to edit drawings as well as
 * perform certain actions on papers.
 */
class Menu extends Plugin {

}

Plugin.register(Menu, false, {
});
